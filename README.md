# wallet-message-pay
Replaced the @/components/ui/input-otp import (which is not available) in OTPVerification with a self-contained 6-box numeric OTP input built from native inputs, including paste distribution, backspac
